# Build environment variables for Supabase Docker images
# These variables are loaded by the chalk-and-push workflow

# Add any build-time environment variables needed for your Docker builds
# For example:
# NODE_ENV=production
# BUILD_VERSION=1.0.0

# If your Dockerfiles require specific ARGs, define them here
# They will be automatically loaded into the GitHub Actions environment